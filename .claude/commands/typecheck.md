---
description: Run TypeScript typecheck (tsc --noEmit).
---

Run:

```bash
npm run typecheck
```

On success, print "typecheck: ok". On failure, print the compiler errors and stop — do not attempt fixes without user direction.
